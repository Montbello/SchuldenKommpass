Title: MVP: Define user flows & endpoints

Description:
Add details from `docs/MVP.md` (user stories, endpoints, minimal acceptance criteria). Reference models in `backend/prisma/schema.prisma` and include a short acceptance test for each user flow.

Acceptance criteria:
- `docs/MVP.md` is reviewed and approved
- List of endpoints and their minimal payloads added
- At least one test case described per user flow

Labels: chore, spec

Notes:
Start with the 3 core flows (Onboarding/Profile, Matching/Tasks, Progress/Proof). Assign to backend team.